import { Context } from 'grammy';

export async function feesCommand(ctx: Context) {
  await ctx.reply(
`💰 *Solnado Cash Fees*

*Fee Structure:*
~0.007 SOL fixed + 0.35% of amount

*Examples:*
┌─────────┬───────────┬─────────────┐
│ Amount  │ Total Fee │ Recipient   │
├─────────┼───────────┼─────────────┤
│ 0.1 SOL │ ~7.4%     │ ~0.093 SOL  │
│ 0.5 SOL │ ~1.8%     │ ~0.491 SOL  │
│ 1 SOL   │ ~1.1%     │ ~0.989 SOL  │
│ 5 SOL   │ ~0.5%     │ ~4.975 SOL  │
└─────────┴───────────┴─────────────┘

_Lower % for larger amounts!_`,
    { parse_mode: 'Markdown' }
  );
}

